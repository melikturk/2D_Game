using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum Tager { One_Player, Two_Player,bar }
public class TagManager : MonoBehaviour
{
    // Start is called before the first frame update
    
        public Tager tagger;


 
}
